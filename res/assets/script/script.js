// Changing the defaults
window.sr = ScrollReveal({ reset: true });

// Customizing a reveal set
sr.reveal('section', { duration: 800, distance: '5px', mobile: false, scale: 1, reset: false, opacity: 0.5 });